[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cellpose-mcp"
version = "0.1.5"
description = "MCP server and CLI for AI-powered Cellpose cell segmentation"
readme = "README.md"
requires-python = ">=3.11,<3.13"
license = "BSD-3-Clause"
license-files = ["LICENSE"]
authors = [
    {name = "Suraj Sahu", email = "surajinacademia@users.noreply.github.com"}
]
maintainers = [
    {name = "Suraj Sahu", email = "surajinacademia@users.noreply.github.com"}
]
keywords = ["cellpose", "mcp", "cli", "segmentation", "microscopy", "ai", "image-analysis"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "Operating System :: MacOS",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Image Processing",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "fastmcp>=2.10.3,<3",
    "cellpose==3.1.1.1",
    "numpy>=1.26.0",
    "imageio>=2.34.0",
    "tifffile>=2021.0.0",
    "typer>=0.9.0",
    "rich>=13.0.0",
]

[project.urls]
Homepage = "https://github.com/surajinacademia/cellpose_mcp"
Repository = "https://github.com/surajinacademia/cellpose_mcp"
Documentation = "https://github.com/surajinacademia/cellpose_mcp"
"Bug Tracker" = "https://github.com/surajinacademia/cellpose_mcp/issues"

[project.optional-dependencies]
test = [
    "pytest>=8.4.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "pytest-timeout>=2.2.0",
    "pytest-mock>=3.12.0",
    "pip",
    "build>=1.2,<2",
    "setuptools>=77",
    "wheel",
]
dev = [
    "ruff>=0.12.10",
    "mypy>=1.17.0",
    "black>=24.0.0",
    "pre-commit>=4.3.0",
    "bandit>=1.8.6",
]
all = ["cellpose-mcp[test,dev]"]

[project.scripts]
cellpose-mcp = "cellpose_mcp.__main__:main"
cellpose-mcp-cli = "cellpose_mcp.cli.app:main"
cellpose-mcp-install = "cellpose_mcp.cli.install:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
cellpose_mcp = ["py.typed"]

[tool.pytest.ini_options]
addopts = [
    "-q",
    "--strict-markers",
    "--tb=short",
    "--cov-config=.coveragerc",
    "-p",
    "no:napari",
    "-p",
    "no:napari-plugin-engine",
]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "slow: marks tests as slow (deselect with -m 'not slow')",
    "integration: marks integration tests",
    "unit: marks unit tests",
    "smoke: marks smoke tests for quick validation",
    "install_e2e: isolated venv + wheel + segmentation (CI install job)",
]
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::PendingDeprecationWarning",
]

[tool.ruff]
line-length = 88
target-version = "py311"
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".mypy_cache",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    "__pypackages__",
    "_build",
    "build",
    "dist",
    "node_modules",
    "venv",
    "*vendored*",
    "*_vendor*",
    "*_version.py",
]
fix = false

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
    "SIM", # flake8-simplify
    "TCH", # flake8-type-checking
    "TID", # flake8-tidy-imports
    "Q",   # flake8-quotes
    "S",   # bandit
    "D",   # pydocstyle
    "BLE", # flake8-blind-exception
    "A",   # flake8-builtins
    "ISC", # flake8-implicit-str-concat
    "G",   # flake8-logging-format
    "PIE", # flake8-pie
]
ignore = [
    "E501",   # line too long - let black handle this
    "BLE001", # blind except - needed for robust error handling
    "S101",   # assert detected - allow in tests
    "S110",   # try-except-pass - acceptable for best-effort operations
    "SIM105", # contextlib.suppress - prefer explicit try/except
    "TC003",  # typing-only imports - not critical
    "D401",   # imperative mood - not critical
]

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ruff.lint.per-file-ignores]
"src/cellpose_mcp/cli/install.py" = [
    "S603",
    "S607",
] # CLI intentionally spawns conda / user-selected Python interpreters
"src/cellpose_mcp/cli/app.py" = [
    "B008",
] # Typer command signatures intentionally use Option() defaults
"tests/**" = [
    "S101",
    "S106",
    "S311",
    "S603",
    "D",
] # Allow asserts, hardcoded passwords, random, subprocess, and skip docstrings in tests

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.black]
line-length = 88
target-version = ["py311", "py312"]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
